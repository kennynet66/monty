#include "monty.h"
/**
 * f_stack - prints the top
 * @head: header of the stack
 * @counter: line_number
 * Return: return void
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
